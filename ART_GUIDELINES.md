# 美术资源协作规范（YellowOil）

> 本文档是该 Unity 项目与美术之间的**唯一协作约定**。
> 美术 / 其它接入项目的 AI 请以本文档为准，按以下目录与流程放置资源。

---

## 一、仓库基本信息

- 远程仓库：`git@github.com:hainanliagnzai/YellowOil.git`（SSH）
- 默认分支：`main`
- 项目类型：Unity **2D** 游戏（防守 / 单位 / 卡牌 / UI）
- 仓库空间：GitHub 免费 ~1G。**大体积美术源文件不进仓库**，只存最终可用的成品资源。

---

## 二、Assets 目录约定

```text
Assets/
├─ 0_TempInbox/                 # 美术交接区：所有成品先丢这里，由程序验收后归位
├─ 2_Art/                       # 美术成品资源（可直接被场景/预制体引用）
│   ├─ Unit/                    #   单位（士兵、敌人、角色等）
│   ├─ Building/                #   城墙、塔、建筑等
│   ├─ UI/                      #   界面图标、图集、按钮等
│   ├─ Effect/                  #   特效
│   ├─ Environment/             #   地面、背景、装饰等
│   └─ Audio/                   #   音效 / 音乐
└─ Scenes/                      # 正式场景（美术默认不碰）
```

> 数字前缀用于固定排序：**0 交接 → 2 成品资源**。
> 预留的 `1_` / `3_` 段位预留给其它用途（如源文件归档、程序组装区），便于后续扩展。

---

## 三、美术工作流程（怎么交付）

1. **把成品丢进 `Assets/0_TempInbox/`**，程序验收确认可用。
2. 程序将验收通过的资源**归位**到 `Assets/2_Art/` 下对应的子目录（Unit/Building/UI/Effect/Environment/Audio）。
3. 归位后再由程序在场景 / 预制体中引用该资源。

**要点：**
- 美术只需要关心 `0_TempInbox/` 和 `2_Art/` 两个目录。
- `Scenes/`、以及后续的代码目录属于程序区，美术不直接编辑，除非被明确委托。

---

## 四、命名规范

统一格式：**`类型_名称_用途`**（全英文，小写下划线分隔，避免重名和跨平台路径问题）。

```text
# 示例
Unit_Soldier_Idle        # 单位-名称-用途
Building_Tower_01        # 城墙/建筑-名称-编号
UI_Button_Start          # 界面-名称-用途
Effect_Explosion_Big     # 特效-名称-类型
```

**规则：**
- 全部小写，单词用下划线 `_` 分隔。
- 同一资源放在比它更高层级的语义文件夹里（如所有城墙部件都在 `2_Art/Building/`）。
- 避免空格、中文、特殊字符（`?*/:<>"|` 等）写进文件名。

---

## 五、gitignore 已锁定的忽略规则

以下**源工程 / 中间文件**格式一律不进入仓库（防止撑爆 ~1G 空间），请原地留档即可，无需提交：

```text
*.psd  *.psb  *.ai  *.skel  *.blend
*.max  *.ma   *.mb  *.c4d   *.exr  *.hdr
```

**保留进仓库**：Unity 运行时可用的最终格式（png / jpg / tga / ttf / svg / prefab / mat 等）。

---

## 六、注意事项

- 不要自行在项目里乱建 `Resources/` 目录做运行时加载，资源加载路径由程序统一规划，避免碎片化。
- 提交前先在 Unity 内刷新（让 `.meta` 生成、资源导入完成），再 `git add`。
- 新增目录时，若目录为空，需要放一个占位文件（如 `.gitkeep`）才能被 git 追踪；开过 Unity 后会自动生成同名 `.meta`，两者都要提交。
- 文件较大或不适合进仓库时**尽早提出**，由程序决定是否走 Git LFS 或其它交付方式。